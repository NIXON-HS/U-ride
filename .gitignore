# ===============================================
# Proyecto U-Ride - Monorepo GitIgnore
# Ignite / React Native / Node.js / React
# ===============================================

# 1. Dependencias globales
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 2. Entorno y Seguridad (¡No cargar datos sensibles!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
backend/.env
frontend/.env
mobile/.env
*.pem

# 3. Compilados y Builds (Frontend / Web)
frontend/dist/
frontend/build/
.tsbuildinfo

# 4. Compilados y Builds (React Native / Mobile)
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/android/app/build/
mobile/ios/build/
*.jks
*.keystore
*.p12
*.key
*.mobileprovision

# 5. Generados por el Backend (Node)
backend/logs/
backend/coverage/
coverage/

# 6. Herramientas del Sistema Operativo e IDEs
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 7. Testing (Supertest / Jest local states)
.jest-test-results.json
